# ???? / ???? � ????

???2026-09-03  
??????????????????????????????????????? `smoke_lit_bf02642003`?  
????? `pipeline.delete_run`????????? `RUN_INFO.paper_id` ?????????????????? `create_project`?

## 1. ??????

### 1.1 ??

- ??????????????????????????? `test_runs` ???????? run?
- ??????????????????????????????? `parsed_results` ? `test_runs`?
- `demo_steel`???????????????
- ?????????????????
- ??????? `non_magnetic` / `smoke_lit_bf02642003` ???? runs?

?????

- ??????`GET /api/papers` ????? `paper_id`????????????? runs ????
- ??????`GET /api/projects` ????????`project_config.json` ????overlay ??????????
- ? `demo_steel` ??????? 4xx?????????
- ?????`..`????????????

### 1.2 ????

- ??????????????
- ??? / ??? / ??
- ???????????`script_snapshots/`
- ??????? `Extract_data` ???
- ???????? LLM ????????????

## 2. ???????

### 2.1 ???? `delete_paper(root, project_cfg, paper_id)`

| ??? | ???? |
| --- | --- |
| ???? | `(root / project_cfg["parsed_results"] / paper_id).resolve()`?????? `parsed_results` ??? |
| ?? runs | `list_runs(..., paper_id)` ????? `run_id`???? `delete_run` ???? `rmtree` |

???????????? overlay??? run?????

?? `paper_id`???`.`?`..`???????? `ValueError`?  
????????? run ? `FileNotFoundError`???? 404??  
??????????? runs????? runs ??? ok?

### 2.2 ???? `delete_project(root, workspace_cfg, project_id)`

???`project_id == "demo_steel"` ? `ValueError`????????????????

???????

1. ????? `workspace_cfg["projects"]` ???????? ? 404??
2. ?? `parsed_results` ?????????? workspace ???
3. ?? `test_runs` ?????????? workspace ???
4. ?? overlay ???`configs/projects/<project_id>.json`??????  
   ? `field_config` ??**????**? `configs/fields/<project_id>.json` ??????????????**??**?? `configs/fields/demo_steel.json` ??????????????????????? `overlay` ? `field_config` ?? `configs/projects/<id>.json`???????? `field_config` ?? `configs/fields/<id>.json` ???????????????
5. ? `configs/project_config.json` ? `projects` ???????????????????

???`configs/templates/`?`configs/field_library/`?`script_snapshots/`?`example_data/`?????????? `parsed_results` ??????��**?????? `parsed_results` ????? `example_data/` ?????????**??????????????`demo_steel` ????????????????

### 2.3 ??????

????????????? API ?????????

- ???`non_magnetic`
- ???`smoke_lit_bf02642003`
- ???????? `test_runs/non_magnetic/test/20260902_125405_two_stage`?`20260903_142335_two_stage` ?? paper_id ? run ?????

## 3. API

???? `handle_post` ???? `/api/run_delete` ????????? DELETE ?????

| ?? | Body | ?? | ?? |
| --- | --- | --- | --- |
| `POST /api/paper_delete` | `{ "project", "paper_id" }` | `{ ok, paper_id, deleted_parsed, deleted_runs: [...] }` | 4xx + `{ error }` |
| `POST /api/project_delete` | `{ "project" }`?? `project_id`? | `{ ok, project_id, deleted: [...] }` | 4xx + `{ error }` |

???????? `rmtree` ? `resolve()` + `relative_to(???)`?? `delete_run` ???

????????????????????????? `default_project`????????? `loadProjects`?

## 4. ??

### 4.1 ??

- `renderPaperTable` ????????????`btn-delete-paper`???? `stopPropagation`?????????
- `confirm`?`???????{title?paper_id}????????????????????????`
- ??????? papers???? id ?????? `state.paperId` ???????????????????/???????
- ???`runStatus` ???????????

### 4.2 ??

- ????????????????????????`btnDeleteProject`??
- ????? `demo_steel` ??? `disabled`?`title` ????????
- `confirm`?`???????{name}??{id}????????????????????????????`
- ?????????????? `default_project` ?????????????????

## 5. ????

| ?? | ?? |
| --- | --- |
| `tools/pipeline.py` | `delete_paper`??????? server ? |
| `tools/config_model.py` ? `pipeline` / server | `delete_project`?? `project_config.json` + ???? |
| `tools/workbench_server.py` | ????? |
| `app/index.html` | ?????? |
| `app/app.js` | ??????? |
| `tests/` | ??????????? paper/project ??? demo ?? |

## 6. ????

1. `delete_paper`???? `parsed_results/p1` + run?`RUN_INFO.paper_id=p1`????????????? paper ? run ???
2. ?? `paper_id`?`../x`?? ???????
3. `delete_project`??? workspace ?????? overlay/dirs?????????????
4. `delete_project("demo_steel")` ? ????????
5. API handler?mock/?? root ? `paper_delete` / `project_delete` ???????
6. UI ??????????????????????????????

## 7. ??

- [ ] ????????????????????
- [ ] `demo_steel` ???
- [ ] `smoke_lit_bf02642003` ?????????
- [ ] ??????
